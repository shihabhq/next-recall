const Library = () => {
  return (
    <div>
      This component will not be shown as it is inside _lib folder cuz _ is
      given before the folder name which means it is a private folder.
    </div>
  );
};

export default Library;
